import 'package:flutter/material.dart';

class VoicePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Voice'),
      ),
      body: const Center(
        child: Text('Welcome to the Voice Page!'),
      ),
    );
  }
}
